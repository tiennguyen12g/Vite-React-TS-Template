### Structure
root/
├── certs/                # SSL certs, keys (optional)
├── dist/                 # Auto-generated by Vite during build
├── node_modules/         # Dependencies (auto-managed)
├── public/               # Static assets served as-is
├── src/
│   ├── apis/             # API call wrappers, API clients
│   ├── assets/           # Static images, fonts, icons
│   ├── components/       # Reusable UI components
│   │   ├── auth/
│   │   └── settings/
│   ├── config/           # Config files and the default variable such as: language, theme, layout...
│   │   ├── CONFIG.md
│   │   ├── constants.ts
│   │   ├── env.ts
│   │   └── routes.ts
│   ├── hooks/            # Custom React hooks        
│   │   ├── common_hooks/
│   │   │   └── useDebounce.ts
│   │   └── useToastSeri.ts
│   ├── i18n/             # Translations, language JSON, i18n setup
│   │   ├── context.tsx
│   │   ├── i18next.ts
│   │   ├── index.ts
│   │   └── locales/
│   │       ├── en.json
│   │       └── vi.json
│   ├── layout/
│   │   ├── AuthLayout.tsx
│   │   ├── DashboardLayout.tsx
│   │   └── MainLayout.tsx
│   ├── lib/              # Larger reusable modules (optional)
│   ├── pages/            # Page-level components for routing
│   │   ├── Layout/
│   │   ├── Home/
│   │   ├── Profile/
│   │   ├── Subscribe/
│   │   └── Docs/
│   ├── playground/
│   │   ├── demo/         # Playground / experiment components
│   │   └── examples/     # Example usage, documentation components
│   ├── router/   
│   ├── services/         # Business logic (separate from API)
│   ├── state/            # Business logic (separate from API)
│   │   ├── tanstack_query/
│   │   │   └── QueryProvider.tsx
│   │   └── zustand/
│   ├── styles/           # Global styles, styled-components, theme
│   ├── types/
│   │   ├── api.types.ts
│   │   ├── global.d.ts
│   │   ├── TYPES.md
│   │   └── user.types.ts
│   ├── utils/            # Reusable utility functions, small helper functions
│   └── vite-env.d.ts
│   ├── workers/          # Web workers  
│   ├── App.tsx
│   ├── main.tsx
│   ├── index.css
│   ├── App.css
└── vite.config.ts
├── STRUCTURE.md
├── tsconfig.app.json
├── tsconfig.json
├── tsconfig.node.json
└── vite.config.ts


###🧠 Simple Rule to Know Where a File Belongs
✔ Put inside components/ if:

It can be used in many pages

It is NOT tied to a route

It is UI building block

It is part of a page, not the page itself

✔ Put inside pages/ if:

You navigate to it with React Router

It is a “screen”

It uses multiple components

It is a top-level route or sub-route


#### Examples
components/
  ui/
    Button.tsx
    Input.tsx
    Modal.tsx
  auth/ -> form UI without layout (place left, right or center)
    LoginForm.tsx
    RegisterForm.tsx
  common/
    ErrorBox.tsx
    Loader.tsx

pages/
  HomePage.tsx
  SettingsPage.tsx
  Auth/    -> use with layout
    LoginPage.tsx
    RegisterPage.tsx
  User/
    ProfilePage.tsx
layout/
    MainLayout.tsx
    AuthLayout.tsx
    DashboardLayout.tsx
utils/
  formatDate.ts
  uuid.ts
  classNames.ts
  mathHelpers.ts
  debounce.ts
  storage.ts
  deepClone.ts
  validateEmail.ts
lib/
  axiosClient.ts      (custom axios wrapper)
  fetcher.ts          (API wrapper)
  queryClient.ts      (tanstack client instance)
  formValidator.ts    (large validation module)
  dateLibrary.ts      (date helpers grouped)
state/
  ui/
    useSidebarStore.ts
    useThemeStore.ts
  auth/
    useAuthStore.ts
  queries/
    useUserQuery.ts
    useProfileQuery.ts
    useLoginMutation.ts
2. Config Alias
- tsconfig.json
- vite.config.ts
- tsconfig.app.json
** If the alias "@"" does not suggest in the top when you typing import, go to Vscode and add to setting.json
```bash
  "typescript.preferences.importModuleSpecifier": "non-relative",
  "javascript.preferences.importModuleSpecifier": "non-relative"
```
